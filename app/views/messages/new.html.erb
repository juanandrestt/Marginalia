<%= simple_form_for @message do |f| %>
  <%= f.input :content, placeholder: "Ask a question", required: true %>
  <%= f.button :submit, 'Ask for a recommendation', class: 'btn btn-primary' %>
<% end %>