<div class="container mt-3">
  <div class="d-flex align-items-center mb-4 position-relative">
    <%= link_to :back, class: "btn rounded-pill bg-white position-absolute", style: "color: #751F1F; left: 0;" do %>
      <i class="fa-solid fa-arrow-left"></i>
    <% end %>
    <h3 class="text-center mb-0 w-100" style="color: #0C0C0C;">All your books</h3>
  </div>
</div>
<div class="container">
  <div class="container">
    <div class="row mx-1">
      <% @books.each do |reading| %>
        <%= link_to book_path(reading.book), class: "text-decoration-none" do %>
          <div class="col-16 d-flex mb-3 align-items-center">
          <%= cl_image_tag reading.book.cover.key, class: "rounded-3", style:"width: 130px; height: 180px; object-fit: cover; margin-bottom: 1rem; box-shadow: 6px 6px 10px rgba(0, 0, 0, 0.1);", alt: reading.book.title if reading.book.cover.attached? %>
              <h5 style="font-size: 16px; margin-bottom: 4px; color: #751F1F;" class="fw-medium playfair-display">
                <%= reading.book.title %>
              </h5>
              <p class="text-muted" style="font-size: 14px; margin-bottom: 0;">
                <%= reading.book.author %>
              </p>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
